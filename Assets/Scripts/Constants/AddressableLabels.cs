namespace SplitRun.Constants
{
    public static class AddressableLabels
    {
        // Format with the zone index: string.Format(k_ZoneObstaclesPrefix, zoneIndex).
        public const string k_ZoneObstaclesPrefix = "zone{0}-obstacles";

        public const string k_Characters = "characters";
        public const string k_Vfx        = "vfx";
    }
}
