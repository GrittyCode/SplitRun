namespace SplitRun.LevelDesign
{
    public enum CoopPatternType
    {
        CoopJump,   // pass lane requires a jump (LaneJump); side lanes are Vertical walls
        CoopSlide,  // pass lane requires a slide (LaneSlide); side lanes are Vertical walls
    }
}
